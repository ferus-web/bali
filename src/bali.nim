{.
  error:
    "Do not import `pkg/bali`, import the specific components you require instead.\n" &
    "* If you wish to interpret JavaScript in a quick-and-dirty way, import `pkg/bali/easy`.\n" &
    "* If you wish to parse JavaScript, import `pkg/bali/grammar/prelude`.\n" &
    "* If you wish to fully control the JavaScript runtime, import `pkg/bali/runtime/prelude`."
.}
